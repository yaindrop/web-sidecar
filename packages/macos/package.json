{
  "name": "@websidecar/macos",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rm -rf .build",
    "format": "swiftformat .",
    "lint": "swiftformat --lint .",
    "build": "cd ../frontend && pnpm build && cd ../macos && ./bundle_app.sh",
    "build:app": "./bundle_app.sh",
    "open": "open .build/release/WebSidecar.app",
    "dev": "./.build/release/WebSidecar.app/Contents/MacOS/WebSidecar"
  }
}
